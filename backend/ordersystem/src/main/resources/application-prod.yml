spring:
  config:
    activate:
      on-profile: prod
  flyway:
    # Managed runtime replicas never receive schema-owner credentials.
    # The release migration process explicitly overrides this to true.
    enabled: false
