spring:
  config:
    activate:
      on-profile: demo
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/demo

orders:
  # Keep seeded lifecycle states stable while a recruiter explores them.
  scheduler:
    enabled: false
